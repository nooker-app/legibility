# Generated by cargo-fuzz. The corpus is machine-found inputs and grows without bound; a crash
# artifact that matters becomes a committed regression test under `tests/regressions/` with a
# `#[test]` beside it, which is the form that keeps working after the fuzzer moves on.
corpus/
artifacts/
coverage/
target/
Cargo.lock
